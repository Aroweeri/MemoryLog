Update example auto_memory_log.txt
rename auto_memory_log.txt, add into config file
check OurDate constructor from days, years, months for values that don't make sense.
add test suites
add option in process to allow for manual date setting + addThis setting
Make MemoryLog produce error message if command (like "add", "delete") is not recognized.
