rename auto_memory_log.txt, add into config file
check OurDate constructor from days, years, months for values that don't make sense.
add test suites
Make the optimizer start at ideal date, don't start at minimum possible date.
Replace reading/writing with JAXB xml stuff. have option to remove a lot of code
Don't wait for user input when finishing a quiz with SubjectTester.
